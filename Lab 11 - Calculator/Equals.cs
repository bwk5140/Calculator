using System;

namespace Lab_11___Calculator
{
    public class Equals
    {
        public void DeleteThisIfNotNeeded()
        {
            /*
            nextEntry = calculator.entryDisplay.Text;
            double compare = Double.Parse(nextEntry);

            if (!nextEntry.Equals("0"))
            {
                if (val == 'N')
                {
                    compare *= -1;
                    calculator.entryDisplay.Text = "" + compare;
                    //nextEntry = "" + compare;
                    entryVal2 = compare;
                }
                else if (char.IsDigit(val))
                {
                    if (nextEntry.Contains(".") && compare - (int)compare == 0)
                    {
                        nextEntry += "" + val;
                    }
                    else if (entryVal2 != double.Parse(nextEntry))
                    {
                        nextEntry = "" + val;
                    }
                    else
                    {
                        nextEntry += "" + val;
                    }

                    entryVal2 = Double.Parse(nextEntry);
                    calculator.entryDisplay.Text = nextEntry;
                }
                else if (val == '.')
                {
                    if (!nextEntry.Contains(".") && entryVal2 != 0)
                    {
                        nextEntry += "" + val;
                    }
                    else if (entryVal2 != double.Parse(nextEntry))
                    {
                        nextEntry = "" + 0 + val;
                    }

                    entryVal2 = Double.Parse(nextEntry);
                    calculator.entryDisplay.Text = nextEntry;
                }
                else if (val == ' ')
                {
                    nextEntry = "0";
                    entryVal2 = 0;
                    calculator.entryDisplay.Text = nextEntry;
                }
                else if (val == '<')
                {
                    nextEntry = "0";
                    entryVal1 = 0;
                    entryVal2 = 0;
                    calculator.entryDisplay.Text = nextEntry;
                    calculator.operandsDisplay.Text = "";
                }

            }
            else
            {
                if (char.IsDigit(val))
                {
                    nextEntry = "" + val;
                    entryVal2 = Double.Parse(nextEntry);
                    calculator.entryDisplay.Text = nextEntry;
                }

                else if (val == '.' && !nextEntry.Contains("."))
                {
                    nextEntry += "" + val;
                    entryVal2 = Double.Parse(nextEntry);
                    calculator.entryDisplay.Text = nextEntry;
                }

                else if (val == ' ')
                {
                    nextEntry = "0";
                    entryVal2 = 0;
                    calculator.entryDisplay.Text = nextEntry;
                }
                else if (val == '<')
                {
                    nextEntry = "0";
                    entryVal1 = 0;
                    entryVal2 = 0;
                    calculator.entryDisplay.Text = nextEntry;
                    calculator.operandsDisplay.Text = "";
                }
            }
            */
        }
    }
}
